---
name: slbb-chatcut-oneshot
description: 一键剪辑 9:16 竖屏口播短视频（单条/批量自适应）。给 1 条视频 → 单条全自动出片；给多条视频或一个文件夹 → 自动排队批量剪。固定套路：删废话停顿、1.1x 提速、全屏 B-Roll + 真人右上角画中画、单行黑底白字字幕、铺背景音频、导出 1080P。当用户要"剪这个视频""做一条口播视频""一键剪口播""帮我剪口播""批量剪""剪这些视频""一起剪"时使用。
---

# 口播一键剪辑（单条 / 批量自适应）

把 9:16 竖屏口播视频按固定套路自动剪成成片。**给 1 条就剪 1 条，给多条/文件夹就自动排队批量剪**。小白只负责给素材，其余全自动。

## 第一步：识别模式（自动，不用问）

数一下用户给的主口播视频（A-Roll）数量：

- **1 条视频** → 单条模式，直接走「单条流程」，剪完即出片。
- **多条视频 / 一个文件夹** → 批量模式，先建队列清单，再逐条走「单条流程」。
- 拿不准时，先列目录数清楚主视频数量，再定模式；不要凭感觉猜。

## 共享固定参数（两条线完全一致，不要改）

| 项 | 固定值 |
| --- | --- |
| 画幅 / 帧率 | 9:16 竖屏，1080×1920，30fps |
| 改速 | 1.1x |
| B-Roll 音频 | 静音（不影响口播原声） |
| 画中画 | 右上角 360×640，静音 |
| 字幕 | 黑底白字、简体中文、**同一时间只显示一行** |
| 背景音频 | 用户排好的一段固定音频（BGM + 开场 3 个音效合成），铺满全片；**duck 深度 -6dB、BGM 增益 +6dB**（保证音量够大） |
| 导出 | 1080P，H.264 |

## 字幕硬规则（两条线通用，最重要）

- **同一时间屏幕上只显示一行字幕。**
- 一句超过屏幕边界时**不换行**，而是**断句**：拆成多个短字幕，按时间轴分时出现。
- 断句按语义切，产品名、金额、英文短语、中文复合词不拆到两张卡。

## 单条流程（固定套路）

1. **导入**：导入主口播视频 + 用户素材，跑转写，确认转写完成。
2. **A-Roll**：删掉停顿、重复、口误、卡壳、无内容的前后空白；**开头的气口（开口前的吸气/停顿/空白）必须剪掉，成片第一帧就开始说话**；保留论据和叙事顺序，不为追求短而乱删。
3. **提速**：整段 1.1x，再收紧时间线，确认无缝隙。
4. **B-Roll + 画中画**：B-Roll 按文案语义覆盖到对应画面（全屏），自带音频静音；横屏素材用 `fit:"cover"` 适配。B-Roll 覆盖的区间，在 V2 之上新建 V3 轨放真人 A-Roll 画中画小窗（右上角 360×640，静音 `decibelAdjustment:-60`），与 V1 保持相同 `sourceStartFromInSeconds` 和 `playbackRate`，逐段拆分对齐 V1 边界。
5. **字幕**：黑底白字简体字幕（`backgroundColor:#000000, backgroundOpacity:1, color:#ffffff`），去掉中文逗号（`hidePunctuation:true`），**遵守上面的「字幕硬规则」**。有文案用文案对齐，没文案时 ChatCut ASR 识别质量足够直接使用。
6. **音频**：V1 设为 `anchor`，新建 A1 音频轨设为 `follower`，**设 follower 的同时必须显式传 `audioRouting.duckDepthDb:-6`（浅 duck）**，否则引擎会按时间线响度自动初始化成约 -13dB 的深 duck，导致 BGM 全程太小；给 BGM item 设 `decibelAdjustment:6`（+6dB 增益）。铺用户排好的背景音频（BGM + 开场音效合成一条），从开头铺到 V1 内容结束帧，**必须裁切到与视频内容等长**，设 0.5s 淡入淡出。
7. **导出**：`local_export(format="video", resolution="1080p", fps=30, codec="h264")`，下载到桌面，用 `ffprobe` 验证编码/分辨率/时长。

## 批量流程

### 素材组织（命名规则）

一个主文件夹，里面每条一个子文件夹，编号决定剪辑顺序：

```
口播批量-<主题>/
├── 背景音频.mp3          ← 全批共用这一条，放根目录（可选，缺则问用户要）
├── broll/                ← B-Roll 池，放 3~5 个供挑选
├── 01_<主题>/
│   ├── 主视频.mp4        ← 必填，唯一一个视频
│   └── 文案.txt          ← 可选，有则字幕/删废话更准
├── 02_<主题>/
└── ...
```

规则三条：① 文件夹名 = 编号 + 主题，编号定顺序；② 主视频必填，每文件夹唯一；③ 文案/背景音频/broll 可选。

### 背景音乐

全批共用同一条，放主文件夹根目录，只导入一次，每条复用。缺背景音频时先问用户要，不自己找素材。

### B-Roll 挑选

用户给 3~5 个 B-Roll 池，AI 按**成片时长**（剪完提速后的实际时长）挑数量：

| 成片时长 | B-Roll 数量 |
| --- | --- |
| ≤ 15 秒 | 1 个 |
| 15~40 秒 | 2 个 |
| > 40 秒 | 3 个（封顶，超 1 分钟也 3 个） |

挑选时按内容贴合度选，与口播语义最相关的优先。

### 队列与调度

1. 扫描主文件夹，按编号生成队列清单（记录每条：主视频、文案、状态）。
2. 从 01 开始，**每条建一个独立工程**（一条一工程，防串料），走完整「单条流程」。
3. 导出后用 `ffprobe` 验证，再切下一条。
4. 单条失败：读回是否已落盘，最多重试一次，仍失败则标记「跳过」，继续下一条。
5. **断点续跑**：中途中断，从没剪完的接着来，不从头重复已完成的。
6. 全部跑完，输出**结果清单**：每条「完成 / 跳过 / 失败」+ 成片路径。

### 输出

成片统一到桌面，附一张进度清单。用户抽查不满意的单条，单独重剪那一条，不动其他。

## 输入（用户要给的）

- **单条**：主口播视频（必填）；文案（推荐）；B-Roll 素材 + 对应句子（可选）；背景音频（可选）。
- **批量**：主文件夹（内含 N 个子文件夹）；背景音频一条（共用）；B-Roll 池 3~5 个。

## 执行硬规则（AI 自己遵守，小白不用管）

- 出错、超时、429/5xx：先读回是否已落盘，最多重试一次，还不行就停下告诉用户。
- 工具返回的 ID 原样使用，不自己拼接、不自己推测。
- 素材上传完成（upload ready）才导出，不拿半成品导出。
- 改速前检查范围，不越过素材结尾。
- 每步做完都读回验证，不把「请求发出去了」当成「做完了」。
- 批量模式：一条一工程，绝不把两条塞进同一工程；每条状态实时更新到队列清单。

详细技术操作见 [references/technical-guardrails.md](references/technical-guardrails.md)。
